apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: occurrence-annotation
  labels:
    app: backend
spec:
  type: LoadBalancer  # For local access on Windows
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: backend
