# Remove contest of a rule

Remove contest of a rule

## Usage

``` r
rm_contest_rule(id)
```

## Arguments

- id:

  id of rule to contest.

## Value

A \`list\` information about the rule contested.

## Examples

``` r
if (FALSE) { # \dontrun{
rm_contest_rule(1)
} # }
```
