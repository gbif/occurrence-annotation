# Get comments on rule

Get comments on rule

## Usage

``` r
get_rule_comment(id = NULL)
```

## Arguments

- id:

  the id of the rule

## Value

a \`tibble\` of comments.

## Examples

``` r
if (FALSE) { # \dontrun{
get_rule_comment(1)
} # }
```
