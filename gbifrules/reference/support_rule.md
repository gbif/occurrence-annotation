# Support a rule

Support a rule

## Usage

``` r
support_rule(id = NULL)
```

## Arguments

- id:

  the id of the rule to updated.

## Value

A \`list\` information about the rule supported.

## Examples

``` r
if (FALSE) { # \dontrun{
support_rule(1)
} # }
```
