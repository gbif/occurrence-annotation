# Delete a ruleset

Delete a ruleset

## Usage

``` r
delete_ruleset(id)
```

## Arguments

- id:

  the id of the ruleset

## Value

list of information about deleted ruleset

## Examples

``` r
if (FALSE) { # \dontrun{
delete_ruleset(1)
} # }
```
